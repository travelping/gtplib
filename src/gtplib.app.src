%% -*- erlang -*-
{application, gtplib, [
    {vsn, git},
    {description, "Erlang library for GTP-c v1 and v2"},
    {applications, [kernel, stdlib, lager, gen_socket, gen_netlink]},
    {registered, []}
]}.
